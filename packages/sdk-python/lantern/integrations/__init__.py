"""Optional third-party framework integrations.

Submodules import their framework lazily so the base SDK carries no extra
dependencies. Install extras per integration, e.g. ``pip install lantern-sdk[deepagents]``.
"""
