apiVersion: v1
kind: Service
metadata:
  name: {{ include "lantern.fullname" . }}-gateway
  labels:
    {{- include "lantern.componentLabels" (dict "root" . "component" "gateway") | nindent 4 }}
spec:
  type: {{ .Values.gateway.service.type }}
  ports:
    - port: {{ .Values.gateway.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "lantern.componentSelectorLabels" (dict "root" . "component" "gateway") | nindent 4 }}
